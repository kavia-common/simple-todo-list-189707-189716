# frontend_app

React UI for the Todo List.

- Requires Node.js 18+
- Copy .env.example to .env and set REACT_APP_API_BASE_URL to your backend (default http://localhost:3001)

Install and run:
- npm install
- npm start

The app runs on http://localhost:3000 and talks to the backend API.
